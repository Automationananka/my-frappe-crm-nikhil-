# CACHE_BUSTER=v8 - This comment forces a rebuild
ARG CACHE_BUSTER=v9

# --- Dockerfile for ERPNext on Railway ---
ARG ERPNEXT_VERSION=v15
FROM frappe/erpnext:v15

# Switch to root
USER root

# This new RUN command will force the cache to break
RUN echo "--- Forcing a rebuild to copy entrypoint ---"

# Copy our entrypoint script into the container
COPY entrypoint.sh /entrypoint.sh

# Make the script executable
RUN chmod +x /entrypoint.sh

EXPOSE 8000

# We stay as root
USER root
